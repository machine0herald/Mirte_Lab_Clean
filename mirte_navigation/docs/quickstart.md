# Quickstart for mirte_navigation

This quickstart explains common usage for the `mirte_navigation` package.

## Run (example)
Add any example commands to run nodes or launch files here. Example:

```
# ros2 launch mirte_navigation example.launch.py
```

## Configuration
Document configuration files, parameters, and environment variables.
